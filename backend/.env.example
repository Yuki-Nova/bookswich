# bookswich 后端配置示例。复制为 .env 后填写真实值。
# 数据目录（相对 backend/ 或绝对路径）
DATA_DIR=../data
# MinerU 云 API（解析用）
MINERU_API_KEY=
MINERU_BASE_URL=
# Obsidian vault 根目录（配置后前端出现「导入 Obsidian」按钮，教材自动放入 <vault>/教材/<书名>/）
OBSIDIAN_VAULT_DIR=
